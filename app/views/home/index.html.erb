<div class="jumbotron">
  <div class="container">
    <h1><strong>BlocMetrics</strong></h1>
    <p><strong>An easy API tool which tracks user behavior on your website!</strong></p>
    <% unless current_user.nil? %>
      <p><%= link_to "My Applications", registered_applications_path(current_user), class: 'btn btn-primary btn-lg' %></p>
    <% else %>
      <p><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-lg" %></p>
    <% end %>
  </div>
</div>

